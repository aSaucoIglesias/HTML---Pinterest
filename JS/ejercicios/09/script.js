const numbers = [1, 2, 3, 5, 45, 37, 58];

function sumAll(param) {
    totalSum = 0;
    for (number of param) {
        totalSum += number;
    }
    return totalSum;
}

console.log(sumAll(numbers));